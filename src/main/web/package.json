{
  "name": "web",
  "version": "1.0.0",
  "description": "front-end clinica",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "browser-sync start --server --files 'dist/css/.css, **/.html'",
    "watch:css": "onchange 'static/css' -- npm run css",
    "start": "run-p serve watch:css"
  },
  "author": "nic",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.5",
    "npm-run-all": "^4.1.5",
    "onchange": "^7.1.0"
  }
}
